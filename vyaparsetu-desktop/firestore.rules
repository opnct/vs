rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allows read/write for all users during development. We will lock this down later.
      allow read, write: if true; 
    }
  }
}
